version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecs-dashboard-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Useful for hot-reloading on Windows hosts
      - VITE_PORT=5173
    # Use standard restart policy
    restart: unless-stopped
